# pnpm configuration for AlpaCash Frontend
# This project uses pnpm (see "packageManager" field in package.json).
#
# Note: "legacy-peer-deps" was an npm-only flag and has been removed.
# pnpm handles peer dependencies natively without requiring legacy mode.
